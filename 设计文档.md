# 科研助手 Agent — 实现方案

## 上下文

基于 LangGraph 构建单智能体 + 多工具编排的科研助手。核心定位：「研究探索 Copilot」而非「论文生成器」。采用**功能扩展式**开发：先跑通一个最小可用的 Agent，再在其基础上逐步叠加状态管理、分层记忆、偏好进化、工具调度等能力，最终实现有状态、可中断、可闭环进化的完整 Agent。

- **LLM**: OpenAI 兼容 API（DeepSeek / 通义千问等国产模型）
- **交互**: FastAPI 后端 + React 前端
- **MVP 工具**: 网络搜索、学术搜索（arXiv + Semantic Scholar）、科学计算器
- **开发方式**: 功能扩展式（非分阶段），每个新能力作为对已有 Agent 的扩展叠加

---

## 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| Agent 框架 | LangGraph | 原生支持状态管理、checkpoint、interrupt、工具编排 |
| LLM | OpenAI 兼容 API | 灵活切换国产模型，无代理依赖 |
| 数据库 | SQLite | 零配置、单文件、足够轻量 |
| 向量库 | ChromaDB | 嵌入式运行，无外部服务依赖 |
| 文档解析 | PyMuPDF + python-docx | 成熟稳定 |
| 搜索 API | Tavily（通用）+ arXiv/Semantic Scholar API（学术） | |
| 后端 | FastAPI + SSE streaming | |
| 前端 | React + Vite + TypeScript | |
| 包管理 | pip + venv（已初始化） | |

---

## LangGraph 图结构设计

### 最终图（包含所有扩展功能后）

```
        ┌──────────────────────────────────────────────────┐
        │                                                  │
        ▼                                                  │
START ─► load_context ─► agent_node ─┬─► [有工具调用?] ──► tool_executor ─┐
                                      │                                      │
                                      └─► [无工具调用] ──► generate_response │
                                                                             │
                                      ┌──────────────────────────────────────┘
                                      │  (回到 agent_node 处理工具结果)
                                      │
                                      ▼
                              compression_check ─┬─► [需压缩] ──► memory_compressor ─► END
                                                 │
                                                 └─► [不需压缩] ─► END
```

### 节点职责

| 节点 | 职责 |
|---|---|
| `load_context` | 根据 project_id 加载记忆摘要、偏好配置、检索相关知识片段，注入到当前消息列表 |
| `agent_node` | 核心 LLM 调用：绑定工具定义 + 动态系统提示词（含偏好），决定是调用工具还是直接回答 |
| `tool_executor` | 执行 LLM 选择的工具（搜索/学术/计算器），结果写回状态 |
| `generate_response` | 基于工具结果或 LLM 直接回答，生成最终用户响应（SSE 流式输出） |
| `compression_check` | 判断是否需要压缩记忆（当前消息数超过阈值） |
| `memory_compressor` | 调用 LLM 蒸馏最近的对话为结构化摘要，计入长期记忆 |

### 状态 Schema

```python
from typing import TypedDict, Annotated, Optional
from langgraph.graph.message import add_messages

class AgentState(TypedDict):
    # === 身份 ===
    project_id: str

    # === 对话 ===
    messages: Annotated[list, add_messages]   # LangGraph 原生消息归并
    summary: str                               # 压缩后的长期记忆摘要

    # === 检索 ===
    retrieved_docs: list[dict]                 # RAG 检索到的文档片段
    search_results: list[dict]                 # 本轮工具搜索结果

    # === 偏好 ===
    global_preferences: dict                   # 全局偏好（所有主题通用）
    project_preferences: dict                  # 当前主题偏好

    # === 控制 ===
    pending_tool_calls: list[dict]             # 待执行的工具调用
    needs_response: bool                       # 是否需要生成最终回复

    # === 人机交互 ===
    feedback: Optional[dict]                   # 用户反馈（采纳/不采纳/快捷标签）
```

---

## 功能扩展路线（在同一个 Agent 上逐层叠加）

### 第 1 步：最小可用 Agent（~200 行）

**目标**: LangGraph 图跑通，一个 LLM 节点能对话

- `graph/state.py` — AgentState 定义（初始仅 messages 字段）
- `graph/builder.py` — 构建图：START → agent_node → END
- `graph/nodes.py` — agent_node：调用 LLM，流式返回
- `api/server.py` — FastAPI，`POST /chat`，SSE 流式输出
- `api/models.py` — 请求/响应 Pydantic 模型

**验证**: `curl -X POST /chat -d '{"message":"你好"}'` 能流式收到回复

---

### 第 2 步：状态持久化 + 项目隔离

**目标**: 对话可中断、恢复；多项目隔离

- 新增 `projects/manager.py` — 项目的 CRUD（SQLite）
- 新增 `memory/store.py` — 对话历史的 SQLite 存储和读取
- `graph/builder.py` — 添加 SQLite checkpointer（LangGraph 原生支持）
- `api/server.py` — 新增 `POST /projects`、`GET /projects/{id}/history`
- State 扩展：添加 `project_id`

**验证**: 创建项目 → 对话 → 关闭 → 重新打开 → 历史消息还在

---

### 第 3 步：工具调用

**目标**: Agent 能自主判断并调用搜索/学术/计算器

- 新增 `tools/web_search.py` — Tavily API 封装
- 新增 `tools/academic.py` — arXiv + Semantic Scholar API
- 新增 `tools/calculator.py` — 白名单科学计算函数
- `graph/nodes.py` — agent_node 绑定工具定义，tool_executor 执行工具
- `graph/router.py` — 条件边：有 tool_call → tool_executor，否则 → generate_response
- State 扩展：添加 `pending_tool_calls`、`search_results`
- `api/server.py` — SSE 事件中区分 thinking/tool_call/response

**验证**: 问 "最新的 AI Agent 论文有哪些"，能看到搜索过程 + 结果

---

### 第 4 步：分层记忆管理

**目标**: 自动压缩历史，长对话不爆上下文窗口

- 新增 `memory/compressor.py` — 调用 LLM 蒸馏对话为结构化摘要
- `graph/nodes.py` — 新增 compression_check 节点和 memory_compressor 节点
- State 扩展：添加 `summary`
- `graph/builder.py` — 在 generate_response 后添加条件边到 compression_check
- `memory/store.py` — 摘要持久化

**验证**: 对话 20+ 轮后，上下文窗口未爆，Agent 仍记得早期关键信息

---

### 第 5 步：偏好进化

**目标**: 采纳/不采纳反馈 + 显式配置，驱动 Agent 行为逐步贴近用户习惯

**偏好分类**（四类结构化 JSON）：

| 分类 | 字段 | 说明 |
|---|---|---|
| `literature` | source_type, year_start/end, paper_type, min_citations, preferred_venues, preferred_language | 文献检索偏好 |
| `writing` | sentence_style, figure_norm, abstract_style, ref_format, lang | 论文写作偏好 |
| `experiment` | metrics, require_control, viz_tool, significance_test, require_ablation | 实验分析偏好 |
| `tool` | prefer_python, prefer_arxiv, avoid_cnki, search_priority, plot_library | 工具调用偏好 |

**两条变更路径**：

1. **显式配置** — `PUT /projects/{id}/preferences` 直接设置 JSON，Pydantic 校验
2. **隐式进化** — `POST /feedback` 提交标签（如「太啰嗦」），通过规则表自动映射到偏好字段，加权调整

**新增文件**：
- `preferences/models.py` — 四类偏好 Pydantic schema + 整体 `PreferencesConfig`
- `preferences/manager.py` — SQLite CRUD + 29 条反馈标签 → 偏好字段映射规则
- `preferences/prompt_builder.py` — 结构化 JSON → 自然语言指令，格式化为「文献检索偏好：...；论文写作偏好：...」

**修改文件**：
- `graph/state.py` — 添加 `system_prompt` 字段（`load_context` 构建后写入，`agent_node._build_messages` 读取）
- `graph/nodes.py` — 新增 `load_context_node`：加载偏好、拼接完整系统提示词（SYSTEM_PROMPT + 摘要 + 偏好指令）
- `graph/builder.py` — `START → load_context → agent_node → ...`
- `memory/store.py` — 新增 `project_preferences` 表（JSON 字段）
- `api/models.py` — 新增 `UpdatePreferencesRequest`、`FeedbackRequest`
- `api/server.py` — 新增 `GET/PUT /projects/{id}/preferences`、`POST /feedback`

**验证**: 
- `PUT /preferences` 设置 `{"writing": {"sentence_style": "concise"}}` → 后续回答明显更简洁
- `POST /feedback {"tag": "需要英文文献"}` → `literature.preferred_language` 自动调整为 `english`

---

### 第 6 步：文件上传 + RAG

**目标**: 上传 PDF/Word 文档后，对话时自动检索文件内容

- 新增 `tools/file_rag.py` — 文档解析 (PyMuPDF + python-docx) + ChromaDB 索引 + 检索
- `api/server.py` — 新增 `POST /projects/{id}/upload`
- `graph/nodes.py` — load_context 中加入向量检索，结果写入 retrieved_docs
- State 扩展：添加 `retrieved_docs`

**验证**: 上传一篇论文 PDF → 提问 "这篇论文的方法论是什么" → 基于文件内容回答

---

### 第 7 步：研究报告导出

**目标**: 一键生成当前研究主题的结构化 Markdown 报告

- 新增 `export/report.py` — 从对话历史 + 记忆摘要 + 引用来源拼装报告
- `api/server.py` — 新增 `POST /projects/{id}/export`
- 报告结构：研究脉络 → 已确认结论 → 待解决问题 → 引用来源

**验证**: 做完一轮研究讨论后，导出的报告内容准确、结构清晰

---

### 第 8 步：前端界面

**目标**: 可演示的 Web UI

- React + Vite + TypeScript 项目（放在 `frontend/`）
- 页面：
  - 项目列表页（创建、切换、删除项目）
  - 对话页（聊天界面 + SSE 流式渲染）
  - 工具调用过程可视化（可折叠的 thought/tool_call 卡片）
  - 采纳/不采纳按钮 + 快捷标签
  - 文件上传区域
  - 报告导出按钮

---

## 目录结构

```
ResearchAssistantAgent/
├── src/
│   ├── __init__.py
│   ├── graph/
│   │   ├── __init__.py
│   │   ├── state.py          # AgentState TypedDict
│   │   ├── nodes.py          # 所有图节点函数
│   │   ├── router.py         # 条件边路由函数
│   │   └── builder.py        # 构建并编译 LangGraph 图
│   ├── tools/
│   │   ├── __init__.py
│   │   ├── base.py           # 工具基类 / 注册表
│   │   ├── web_search.py     # Tavily 网络搜索
│   │   ├── academic.py       # arXiv + Semantic Scholar
│   │   └── calculator.py     # 科学计算器
│   ├── memory/
│   │   ├── __init__.py
│   │   ├── store.py          # SQLite 对话存储 + 摘要 CRUD
│   │   └── compressor.py     # LLM 蒸馏摘要
│   ├── preferences/
│   │   ├── __init__.py
│   │   ├── manager.py        # 偏好 CRUD
│   │   └── prompt_builder.py # 动态 system prompt
│   ├── projects/
│   │   ├── __init__.py
│   │   └── manager.py        # 项目 CRUD
│   ├── export/
│   │   ├── __init__.py
│   │   └── report.py         # Markdown 报告生成
│   └── api/
│       ├── __init__.py
│       ├── server.py          # FastAPI 应用
│       ├── models.py          # Pydantic 请求/响应模型
│       └── sse.py             # SSE 流式输出工具
├── frontend/                  # React 前端（第 8 步创建）
├── data/                      # 运行时数据（SQLite、ChromaDB、上传文件）
├── requirements.txt
└── README.md
```

---

## 关键设计决策

### 1. 为什么是单 Agent 而非多 Agent？

LangGraph 的单 Agent + 多工具模式足够覆盖当前需求。每个工具是独立函数，Agent 自主路由选择。避免多 Agent 的额外复杂度（Agent 间通信、状态同步、调度顺序），同时保留了扩展性——未来如果某条链路真的需要独立的 Agent，可以从这个基座上拆出来。

### 2. checkpointer 如何实现中断恢复？

LangGraph 的 `SqliteSaver` 原生支持。每次图执行到节点后自动保存状态快照。用户关闭页面、次日重新打开时，通过 `thread_id`（映射到 project_id）恢复到最后一次 checkpoint。

### 3. 记忆压缩的时机和策略

- **触发条件**：当前 messages 列表超过 20 条（可配置）
- **压缩策略**：保留最近 5 轮对话原文，其余交由 LLM 蒸馏为结构化摘要
- **摘要格式**：`{核心结论、关键信息点、未解决问题、时间戳}`
- 原始对话永久保留在 SQLite，摘要只是进入 LLM 上下文

### 4. 偏好如何生效？

每次 `load_context` 时检索当前主题偏好 + 全局偏好，拼入 system prompt：

```
你是科研助手。当前主题偏好：[偏好列表]。全局偏好：[偏好列表]。
请遵循以上偏好回答问题。
```

反馈（采纳/不采纳/快捷标签）通过 `POST /feedback` 传入，偏好管理器更新权重（简单加权计数）。

### 5. 工具调度策略

不引入额外的路由器 LLM。直接用 agent_node 绑定所有工具定义，靠 function calling 原生路由：

- 问 "最新" → LLM 判断需要搜索 → 调 web_search
- 问 "论文" → LLM 判断需要学术检索 → 调 academic_search
- 问 "计算" → LLM 判断需要计算 → 调 calculator
- 问 "文件里" → 向量检索在 load_context 预先完成，结果放入上下文

---

## 验证方案

每个功能扩展步骤完成后，运行对应验证：

1. `curl POST /chat` → 流式返回
2. 创建项目 → 多轮对话 → 重启服务 → 历史恢复
3. 提问触发工具调用 → 确认搜索/学术/计算器执行
4. 长对话不爆上下文 → 早期信息仍可回忆
5. 标记偏好后 → 回答风格改变
6. 上传 PDF → 基于文件内容回答
7. 导出报告 → 内容准确
8. 前端完整交互流程

---

## 未来扩展

### 多 Agent 协作

当前为单 Agent + 多工具模式，适用于 MVP 阶段。当科研任务复杂度上升时（如检索、深度阅读、批判性评估、结构化写作需要不同视角），可引入 supervisor 多 Agent 架构：

```
load_context
     │
     ▼
supervisor_agent (调度者)
     │
     ├──► search_agent      网络搜索 + 学术检索
     ├──► analyst_agent     数据分析 + 计算 + 可视化
     ├──► reader_agent      文献阅读 + 摘要 + 批判性评估
     ├──► writer_agent      结构化写作 + 引用管理
     └──► reviewer_agent    质疑结论 + 发现逻辑漏洞 + 给出反对观点
     │
     ▼
generate_response → compression_check → END
```

- supervisor 负责任务分解和调度，各 agent 有独立的 system prompt 和工具集
- agent 间通过 supervisor 汇总结果，避免直接通信的复杂度
- 与设计文档第 1 节「为什么是单 Agent」中的扩展预留一致

### 观测层

在 Agent 图执行外围增加观测中间件，不改变核心逻辑，提供以下能力：

| 能力 | 说明 | 实现思路 |
|------|------|---------|
| **全链路轨迹日志** | 记录每一步思考、工具调用、记忆检索、压缩记录 | LangGraph checkpoint 已记录状态快照，加一层日志中间件采集节点入口/出口、工具参数/结果、LLM 原始响应 |
| **Token 消耗统计** | 每次 LLM 调用记录 prompt_tokens / completion_tokens，按会话和节点维度聚合 | LLM response 自带 `usage` 字段，中间件采集后写入 SQLite 统计表或单独的分析数据库 |
| **耗时追踪** | 记录每个节点的执行耗时，定位瓶颈 | 中间件在节点执行前后打时间戳，区分 LLM 耗时和工具耗时 |
| **失败率与重试** | 统计工具调用失败、LLM 调用超时、异常退出的次数 | 中间件捕获异常并分类计数，按 project_id + tool_name 维度聚合 |
| **上下文溢出检测** | 当 messages 数量超过 compression_threshold 但压缩未生效时告警 | 在 compression_check 节点记录上下文窗口使用率，接近上限时触发告警 |
| **回放与评估** | 完整会话轨迹可复现，自动打分评估任务完成质量 | 利用 LangGraph thread_id + checkpoint 重放会话；另起一个评估 LLM 对回答质量打分（准确性、完整性、相关性） |
| **告警机制** | 死循环、高频报错、超限调用自动触发 | 在 supervisor 或图执行外层加超时计数器和错误阈值检测，触发时写日志或回调通知 |

观测层可独立于多 Agent 改造实施——当前单 Agent 架构下即可接入，未来切到多 Agent 时观测能力平滑继承。
