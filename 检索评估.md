你的描述大致对，但有一个关键点要纠正：不是"一次 LLM 调用同时评两个指标"，而是两次不同粒度的 LLM 调用分开评，而且规则信号根本不用 LLM。逐条拆开（依据 llm_assess.py）：

一、四个维度 ≠ 一个 LLM 一次评完
维度	谁评	怎么调
权威性	纯规则（零 LLM）	只看 source_type + url 域名，不走 LLM
时效性	纯规则 + 用户问题	看 published 年份 + 问题是否含时效词，不走 LLM
相关性	LLM 阶段1	N 次并行，每条来源一次独立调用
一致性	LLM 阶段2	1 次全局调用，顺带出缺口/小结/needs_refetch
二、阶段1（相关性）：不是一次调用，是每条来源一次
assess_relevance 里对每条来源 _one(source) 单独拼 prompt 后 asyncio.gather 并行：




RELEVANCE_PROMPT:
  用户问题：{question}
  来源内容：
    标题：{title}
    类型：{source_type}
    摘要：{summary}   ← 截断：前 500 字符
→ 输出：{relevance: 0-5, reason: 一句话}
所以"用户问题 + 单条来源信息 + 摘要"这个组合是真的，但对象是一条来源，N 条来源要 N 次并行调用。

三、阶段2（一致性/缺口）：这次才是"问题 + 全部来源"一次全局调用
assess_global 先压缩来源列表，再单次调用：

Python



one_line = summary.split("\n")[0][:120]     # ← 每条只留第一行、≤120 字符
lines.append(f"[{num}] {title} | {one_line}")
prompt = GLOBAL_PROMPT.format(question=user_query, sources_text=sources_text)



GLOBAL_PROMPT:
  用户问题：{question}
  来源列表（编号 | 标题 | 一句话结论）：
    [1] xxx | 第一行摘要
    [2] xxx | 第一行摘要
→ 输出：逐条一致性分 + gaps + needs_refetch + summary
可见一致性才是"问题 + 全部来源"一起交给一个 LLM 的，而相关性是逐条的。

四、关于截断——确实有，且发生在两个层级
截断一共三层，别混：

解析层（parser.py:51）：工具输出落进 reference_sources 时，文档来源的 summary 只留 50 字符（web/paper 保留全文内容存 summary）；
阶段1 prompt 层（llm_assess.py:155）：拼相关性 prompt 时 summary[:500]——即使来源里存了全文，喂给 LLM 也只取前 500 字符；
阶段2 prompt 层（llm_assess.py:179）：拼全局 prompt 时把 summary 压成第一行 ≤120 字符。
也就是说：web/paper 来源的完整正文存在 state 里，但 LLM 两阶段都只看截断摘要（500 字 / 第一行 120 字），为的是控制单次 prompt 体量与成本。

五、修正后的完整画面



source（含 summary，可能达几千字）
   │
   ├─ 规则：authority(域名/类型) + timeliness(年份+时效词)   ← 零 LLM，不看摘要
   │
   ├─ LLM 阶段1：N 条并行，每条 (问题 + 标题 + 类型 + summary[:500]) → 相关性分
   │
   └─ LLM 阶段2：1 次全局，(问题 + 全部 [编号|标题|首行摘要≤120字]) → 一致性分 + 缺口 + 小结
结论："用户问题 + 来源 + 摘要一起交给 LLM 评"对一致性成立（一次全局），对相关性不成立（是 N 次并行的逐条调用）；规则两维则完全不走 LLM。截断确实存在：解析层 50 字符（文档）、阶段1 取前 500、阶段2 只留第一行 120 字符。